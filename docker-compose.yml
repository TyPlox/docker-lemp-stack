version: '2'

services:
  nginx:
    image: nginx:alpine
    restart: always
    links:
        - 8-1-fpm
        - 8-0-fpm
        - 7-4-fpm
    ports:
        - "80:80"
        - "443:443"
    volumes:
        - ./data/nginx/enabled:/etc/nginx/conf.d
        - ./data/nginx/snippets:/nginx/snippets
        - ./data/nginx/certs:/nginx/certs
    volumes_from:
        - data

  8-1-fpm:
    build: build/php/8-1
    restart: always
    volumes_from:
        - data

  8-0-fpm:
    build: build/php/8-0
    restart: always
    volumes_from:
        - data

  7-4-fpm:
    build: build/php/7-4
    restart: always
    volumes_from:
        - data

  db:
    image: mysql:8.0.32
    restart: always
    environment:
          MYSQL_ROOT_PASSWORD: root
    volumes:
        - ./data/db:/var/lib/mysql
    ports:
      - "3306:3306"


  dbadmin:
    image: phpmyadmin/phpmyadmin
    restart: always
    environment:
      PMA_HOST: db
      PMA_USER: root
      PMA_PASSWORD: root
      UPLOAD_LIMIT: 64M
    depends_on:
      - db

  mongodb:
    image: mongo:6.0.3
    ports:
      - "27017:27017"
    volumes:
      - ./data/mongo:/data/dbmongo
    environment:
      - MONGO_INITDB_ROOT_USERNAME=aquino
      - MONGO_INITDB_ROOT_PASSWORD=pass1234
    restart: unless-stopped

  data:
    image: alpine:latest
    command: echo "READY!!!"
    volumes:
      - ./data/vhosts:/vhosts
      - ./data/tmp:/tmp
